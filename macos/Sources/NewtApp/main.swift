import NewtKit

// Phase 0 stub: proves the Swift shell links and calls the Rust core.
// Phase 3 replaces this with an NSApplication, a window, and the renderer.
print("newt shell — core \(Core.version)")
