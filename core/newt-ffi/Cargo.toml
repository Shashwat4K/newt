[package]
name = "newt-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "C ABI surface over newt-core."

[lib]
name = "newt_ffi"
crate-type = ["staticlib", "rlib"]

[dependencies]
newt-core = { workspace = true }

[build-dependencies]
cbindgen = "0.29"
